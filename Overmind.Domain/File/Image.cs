namespace Overmind.Domain.File
{
    public class Media : Abstract.File
    {
        public Media(string fileId)
            : base(fileId)
        {
            // Blank
        }

        public override void Type(string type)
        {
            // Decide type
        }
    }
}
